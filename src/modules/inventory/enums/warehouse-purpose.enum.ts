export enum WarehousePurpose {
  SALEABLE = 'saleable',
  RESERVE = 'reserve',
  DAMAGED = 'damaged',
  RETURNS = 'returns',
  TRANSIT = 'transit',
  PRODUCTION = 'production',
  GENERAL_STORAGE = 'general_storage',
}
