export enum DispatchExpenseType {
  FUEL = 'fuel',
  TOLLS = 'tolls',
  PER_DIEM = 'per_diem',
  DRIVER_PAY = 'driver_pay',
  HELPER = 'helper',
  OUTSOURCE = 'outsource',
  MAINTENANCE = 'maintenance',
  OTHER = 'other',
}
